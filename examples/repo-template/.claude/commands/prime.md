# Purpose

Understand this codebase and report your understanding.

## Workflow

1. @README.md
2. Run `git ls-files` to list all files in the repository.
3. Run `git log --oneline --graph --decorate --all` to view the project's commit history.
4. @ai-docs/handoff.md


